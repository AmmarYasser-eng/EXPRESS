# User API Assignment using express

This project is a simple REST API built with Node.js and Express.  
It allows fetching a user by name, updating a user by ID, and deleting a user by ID.  

The repository also includes a Postman collection with examples for testing all endpoints.
